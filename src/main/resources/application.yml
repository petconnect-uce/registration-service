spring:
  application:
    name: registration-service

  datasource:
    url: jdbc:mysql://localhost:3306/petconnect_users
    username: root
    password: 7118

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8082
